import ScrollReveal from "scrollreveal";

export default new ScrollReveal({
    reset: true,
    delay: 500,
    distance: "100%",
    duration: 1000,
    rotate: {
        x: 300,
        z: 200,
    },
    scale: 0.3,
});
